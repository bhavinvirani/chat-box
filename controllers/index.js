const authController = require('./auth.controller');
const userController = require('./user.controller');
const chatController = require('./chat.controller');

module.exports = { authController, userController, chatController };
